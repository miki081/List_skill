<p id="notice"></p>
  <div class="container">
   <div class= "col-sm-12 col-md-8 col-lg-5 px-sm-0 mx-auto">
<h3>イベント開催編集</h3>
<%= form_with model: @event, local:true do |f| %>
 <div class="form-group">
   <label for="event_title">Title</label>
   <p><%= f.text_field :event_title %></p>
 </div>

 <div class="form-group">
  <label for="event_capital">Capital</label>
    <p><%= f.text_area :event_capital %></p>
    <a>※必ず日付、時間を入れてください</a>
 </div>

 <div class="form-group">
   <%= f.label :post_image %>
    <%= f.file_field :post_image, class: 'form-control mb-3', accept: 'image/*',　size: '100x100' %>
    <%= f.hidden_field :post_image_cache %>
    ※イベントのイメージが出るような写真を投稿してください。
 </div>
 
 <div class="form-group row">
    <%= f.label :ジャンル, class: "col-sm-4" %>
    <%= f.collection_select :genre_id, Genre.all, :id, :name, class: "form-control col-sm-8" %>
  </div>
  
  <div class="form-group">
   <%= f.label :post_code %></br>
   <%= f.text_field :post_code %>
  </div>
  
  <div class="form-group">
   <%= f.label :address %></br>
   <%= f.text_field :address %>
  </div>


  <div class="form-group"></br>
    <input type="submit" name="commit" value="内容を変更する" class="btn btn-success" data-disable-with="内容を変更する" />
  </div>
 <% end %>
 </div>
 </div>
 </div>