 <p id="notice"></p>
  <div class="container">
   <div class= "col-sm-12 col-md-8 col-lg-5 px-sm-0 mx-auto">
<h3>新規Event開催</h3>
 <%= form_with model: @event, local:true do |f| %>
 <div class="form-group">
   <label for="event_title">Title</label>
   <p><%= f.text_field :event_title %></p>
 </div>

 <div class="form-group">
  <label for="event_capital">Event紹介</label>
    <p><%= f.text_area :event_capital %></p>
    <a>※必ず日付、時間を入れてください</a>
 </div>

 <div class="form-group">
   <%= f.file_field :images, multiple: true, class: "size" %></br>
    ※イベントのイメージが出るような写真を投稿してください。
 </div>
 
 <div class="form-group">
    <%= f.label :ジャンル, class: "col-sm-4" %><br/>
    <%= f.collection_select :genre_id, Genre.all, :id, :name, class: "form-control col-sm-8" %>
  </div>
  
  <div class="form-group">
   <%= f.label :post_code %><br/>
   <%= f.text_field :post_code %>
  </div>
  
  <div class="form-group">
   <%= f.label :address %><br/>
   <%= f.text_field :address, placeholder: "住所" %>
  </div>

  <div class="form-group"><br/>
    <input type="submit" name="commit" value="投稿する" class="btn btn-success" data-disable-with="投稿する" />
  </div>
 <% end %>
</div>
</div>
</div>
